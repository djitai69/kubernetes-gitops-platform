apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: node-api-local
  namespace: node-api-staging
spec:
  provider:
    kubernetes:
      remoteNamespace: node-api-staging
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          namespace: node-api-staging
          key: ca.crt
      auth:
        serviceAccount:
          name: eso-local-reader
