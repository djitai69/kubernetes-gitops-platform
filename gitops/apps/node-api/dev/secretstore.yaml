apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: node-api-local
  namespace: node-api-dev
spec:
  provider:
    kubernetes:
      remoteNamespace: node-api-dev
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          namespace: node-api-dev
          key: ca.crt
      auth:
        serviceAccount:
          name: eso-local-reader
