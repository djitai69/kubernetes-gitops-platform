apiVersion: v1
kind: Namespace
metadata:
  name: node-api-dev
  labels:
    kubernetes.io/metadata.name: node-api-dev
---
apiVersion: v1
kind: Namespace
metadata:
  name: node-api-staging
  labels:
    kubernetes.io/metadata.name: node-api-staging
---
apiVersion: v1
kind: Namespace
metadata:
  name: node-api-production
  labels:
    kubernetes.io/metadata.name: node-api-production
---
# Created here (not left to kube-prometheus-stack's createNamespace) so the
# metrics NetworkPolicy allow-rule targets a namespace that exists from the
# core demo onward, independent of whether the observability profile
# (make demo-observability) has been run yet.
apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels:
    kubernetes.io/metadata.name: monitoring
